export interface customThemeDto {
  colors: {
    text: string;
    placeholder: string;
    primary: {
      black: string;
      skyblue: string;
      brown: string;
      yellow: string;
      orange: string;
      green: string;
      lightgreen: string;
      error: string;
    };
  };
}
